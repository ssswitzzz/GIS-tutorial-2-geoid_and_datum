# Workspace Guidelines & Rules

## Python & Manim Execution Environment
- Always use `uv run python` and `uv run manim` for executing Python scripts and rendering Manim videos in this workspace.
- Do NOT call external or system Python environments directly.
